# 0.1.0-beta.2 本地验收记录

日期：2026-08-28。工作开始基线为 `71fbbfd`；交付时 `codex/data-update-release` 的 HEAD 为 `a995e84`（并行文档任务提交），叠加本任务未提交改动。本地 APK 来自该工作过程中的未提交工作树，不归因于干净 commit；本记录不是已发布版本或发布准备回执。原有改动保留；本任务没有提交、推送或配置 GitHub。

## 已交付

- 网页与 Android 的两种导出、v1/v2 校验、只读预览和确认后事务合并。
- Android 自动／手动检查、显式下载与取消、缓存校验、安装来源授权和系统安装器交接；更新器独立复核发现的安装时序、通知关闭与缓存恢复问题均已修复并复核通过。
- MIT、统一版本配置、public 空题库构建、默认 dry-run 的本地 Release／Pages 发布脚本，以及[使用指南](../data-transfer-and-updates.md)。

## 自动验证

| 检查 | 本次结果 |
| --- | --- |
| `python -m pytest test -q` | 最终 475 passed，138.74 秒 |
| `node --test test/speech_input.test.cjs` | 26 passed，无失败／跳过 |
| `android.ps1 -Mode Build` | BUILD SUCCESSFUL，55 秒；包含 public release、Java 单测、lint |
| Gradle Java XML 报告 | 55 tests，0 failures，0 errors |
| release lint | 0 errors，4 warnings |
| 精确 public APK | 版本／包名／ABI、签名、ZIP 对齐、原生 ELF、资源允许列表和空种子校验通过 |
| 发布脚本测试 | 34 passed，已包括在最终 pytest 中，不另计总数 |
| 默认发布预检 | 正确拒绝脏工作区；没有访问发布凭据或写远端 |

Java 分项为 HostPolicy 6、SpeechInput 17、UpdateEngine 22、UpdateInstallGate 4、UpdatePolicy 6。原生直接编译测试使用实际 Android API 与临时 R/BuildConfig stub，不再依赖历史构建输出。

lint 警告为 `UnusedAttribute`、`ObsoleteSdkInt`、`StaticFieldLeak`、`MonochromeLauncherIcon`。其中更新控制器静态实例持有的是 application Context，Activity 使用弱引用；保留该警告，不把 lint 描述为零警告。另有 SDK XML 工具版本与 Gradle／Java 弃用提示。

早期一次回归出现 Windows loopback 连接中断；针对该鉴权矩阵重跑 8 项通过，之后完整回归通过。没有为绕过它修改鉴权或失败断言。

## 隔离设备验证

使用新建的 API29/API36 Google APIs x86_64 模拟器；未安装、清除或修改已有用户设备。测试只使用合成题目、假模型配置和模拟模型响应，自动更新网络查询在测试启动前关闭。

- 两台设备均完成相同签名 QA1001 → QA1002 → QA1003 的 `adb install -r` 真实覆盖安装，未清数据。
- 每次升级前后逐项比对题库、复习进度、会话／评分分析、配置、假 Key 文件、SQLite 索引，以及 5 项原生状态／草稿；断言均通过。
- QA1002：两台设备各通过文件选择取消、损坏备份拒绝、确认后导入、纯题库预览后 Activity 重建仍要求确认、文件被替换后仍使用先前校验字节，以及专用 APK Provider 只读边界测试。
- 最终 QA1003：两台设备各 3 项通过，覆盖升级数据保留、有 open 会话时真实 SAF 导入拒绝、只读 Provider 边界。
- 跨端迁移：API29 隔离设备 QA1002 经真实 SAF 导出 2 题，桌面真实页面预览并确认导入；桌面本机 HTTP 导出接口生成含原有 1 题的两种 3 题文件，再由 QA1003 经真实 SAF 选择、预览并确认导入。含进度恢复显示新增 1／更新 2，纯题库恢复显示新增 0／更新 3，Android 总计 5 题、本机独有 2 题保留。以只读合成数据库快照核对分类、题干、答案、URL 及 5 项调度字段：Android 原导出 2 题往返一致，桌面 3 题与 Android 一致；纯题库导入前后整库 SHA-256 相同，5 题进度、4 条会话、4 条会话题目及 SQLite v2 schema／索引不变。证据保留于忽略目录 `dist/android/qa/transfer-20260828/`；桌面导出验证的是实际 HTTP 二进制接口，不冒充浏览器下载落盘交互。

QA APK 单独保存在忽略的 `dist/android/qa/update-20260828/1001`、`1002`、`1003`，不属于公开附件。它们使用测试版本编号和 x86_64 ABI，不能代替最终 ARM64 APK 的真机验收。

## 精确本地候选 APK

位置：`dist/android/0.1.0-beta.2/public/bagu-0.1.0-beta.2-public-arm64-v8a.apk`。

- 版本：`0.1.0-beta.2` / `2`，beta，public。
- 包名：`io.github.ingnijm.baguhelper`；Android 10+，仅 `arm64-v8a`。
- 大小：29,866,145 字节。
- SHA-256：`befd5b1f3f43029e4d87a55fc3d5077be182d9560fb01aed7f7d1972227e55e2`。
- 签名证书 SHA-256：`ac92a24f30a5e6c10c4ced0d0db89124f39f36e00778fef6ca3ba4973bdf0ee3`。
- 种子题目／会话／会话题目均为 0；未打包工作站题库、配置、进度或签名私钥。
- 同目录只有约定六个公开附件；更新清单中的 URL 是预定发布地址，不表示线上文件已存在。

这是直接 Build 生成的本地验收产物，没有 `verification.json`。后续正式 prepare 必须来自已提交源码；不能给这个未提交工作树的 APK 伪造回执。若 prepare 因已有无归属 public 目录停止，应先人工保留／移出该验收目录，再从已提交源码准备，不覆盖旧附件。

## 尚未完成的门槛

1. 未在 ARM64 真机上验收此精确 release APK，也未完成真实 GitHub 下载 → 未知来源授权 → 系统安装确认／取消 → 重启核实的整条更新链。平台 URI 授权撤销及安装中进程死亡仍需该链路验证；单元测试和 adb 覆盖安装不等价。
2. 跨端真实文件的上述 UI／HTTP 路径及逐字段迁移已在 API29 x86_64 合成环境完成；仍未用最终 ARM64 release APK／真实手机复验，也未单独验收桌面浏览器下载落盘交互。不可把该结果扩展为所有真机或全部文件分享路径通过。
3. 未使用 GitHub 发布凭据，未核验线上仓库可见性、现有 Release／最高版本或 Pages 配置；未创建 Tag、Draft、Release 或更新 feed 分支。
4. 维护者安装并登录 gh、确认仓库／版本／附件、审查提交源码和补齐上述验收后，才能执行指南中的显式发布流程。Release、匿名下载与 Pages 必须分别报告结果。

因此本记录证明本地实现、回归和所列 APK／模拟器检查通过，不宣称全部端到端或公开发布完成。
